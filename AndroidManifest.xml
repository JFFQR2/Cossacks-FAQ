<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.river.quite.cossacksfaq">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CountriesActivity" />
        <activity android:name=".CountryActivity" />
        <activity android:name=".TacticksActivity" />
        <activity android:name=".DataActivity" />
        <activity android:name=".TextTactickActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".KnowledgebaseActivity"></activity>
    </application>

</manifest>